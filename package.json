{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd letschat && npm install && npm run build",
    "start":"cd server && npm install && node index.js"
  },
  "author": "",
  "license": "ISC"
}
